name: Auto Remove Dead Versions

on:
  schedule:
    - cron: '43 1/4 * * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

defaults:
  run:
    shell: bash -euo pipefail {0}

jobs:
  update-packages:
    name: Remove Dead Versions
    runs-on: ubuntu-26.04-arm
    environment:
      name: Production (gh only)
      deployment: false
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          persist-credentials: false

      - name: Install komac
        uses: UnownPlain/install-komac@main
        with:
          version: nightly
          repo: unpn-org/Komac
          linux_libc: musl
          compression: zst

      - name: Remove Dead Versions
        run: |
          jq -r '.vanity_url_packages[]' shards/auto-remove.json | while read -r pkg_id; do
            komac list-versions "$pkg_id" --json |
              jq -r '.[0:-1][]' |
              while read -r version; do
                komac remove "$pkg_id" --version "$version" --submit --no-warning --reason 'Old Vanity URL version'
              done
          done

          jq -r '.remove_dead_versions[]' shards/auto-remove.json | while read -r pkg_id; do
            komac remove-dead-versions "$pkg_id" --auto
          done
        env:
          KOMAC_FORK_OWNER: ${{ github.repository_owner }}
          GITHUB_TOKEN: ${{ secrets.UPDATE_TOKEN }}
