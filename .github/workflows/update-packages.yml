name: Update Packages

on:
  schedule:
    - cron: '7 * * * *'
    - cron: '17 * * * *'
    - cron: '28 * * * *'
    - cron: '37 * * * *'
    - cron: '49 * * * *'
    - cron: '58 * * * *'
  workflow_dispatch:

jobs:
  update-packages:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
      - name: Install Dependencies
        run: bun ci --production
      - name: Install Komac (Nightly)
        run: |
          wget -nv -P /usr/local/bin/ https://github.com/russellbanks/Komac/releases/download/nightly/komac-nightly-aarch64-unknown-linux-gnu.tar.gz || \
          wget -nv -P /usr/local/bin/ https://github.com/russellbanks/Komac/releases/download/v2.13.0/komac-2.13.0-aarch64-unknown-linux-gnu.tar.gz
          tar -xf /usr/local/bin/*.tar.gz -C /usr/local/bin/
      - name: Update Packages
        env:
          KOMAC_FORK_OWNER: ${{ github.repository_owner }}
          GITHUB_TOKEN: ${{ secrets.UPDATE_TOKEN }}
        run: bun start
